name: Validate Agent Skill

on:
  push:
    paths:
      - ".github/workflows/validate-skill.yml"
  workflow_dispatch:

jobs:
  install:
    runs-on: ubuntu-latest
    steps:
      - name: Validate public install through skills CLI
        run: npx -y skills add skcache/prnotes -a codex -y
